// SkillCard is no longer used — skills are now rendered
// directly inside Skills.tsx as grouped category cards.
// This file is kept to avoid breaking any residual imports.

import { skill } from '@/types/main';

const SkillCard = (_props: skill) => null;

export default SkillCard;